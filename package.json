{
  "name": "metrology-analyzer",
  "private": true,
  "description": "Metrology Data Analyzer — static ES modules; use a local HTTP server (not file://).",
  "scripts": {
    "dev": "npx --yes serve .",
    "sync-release": "node scripts/sync-release.mjs",
    "release": "node scripts/release.mjs",
    "bundle": "node scripts/sync-release.mjs && node scripts/bundle-html.mjs"
  },
  "devDependencies": {
    "acorn": "^8.14.0"
  }
}
